#pragma once
#include <cstdint>


//circular defination is a complex term made up by nerds
// i dont have enrgy to fix it so imma just extract it to another class
struct Move {
    uint8_t smallidx;
    uint8_t bigidx;
};